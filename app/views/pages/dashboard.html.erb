<h1>Pages#dashboard</h1>
<p>Find me in app/views/pages/dashboard.html.erb</p>

<table class="table">
 <thead>
   <tr>
     <th scope="col">Name</th>
     <th scope="col">Role</th>
       <th scope="col">Modificar Role</th>
     <th scope="col">Eliminar</th>
   </tr>
 </thead>
 <tbody>
   <% @users.each do |user|%>
   <tr>
       <td><%= user.name %></td>
       <td><%= user.role %></td>
       <%= form_with(model: @user, local: true, html: { class: 'form-horizontal' }) do |f| %>
       <%end%>
   </tr>
   <% end %>
 </tbody>
</table>
